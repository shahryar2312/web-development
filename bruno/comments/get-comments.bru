meta {
  name: Get Comments
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/posts/{{postId}}/comments
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  page: 1
  sort: best
  ~parentId:
}
