meta {
  name: Vote Post
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/posts/{{postId}}/vote
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "value": 1
  }
}
