meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
}

body:json {
  {
    "username": "IliaKlodin",
    "email": "IliaKlodin446688@boohoo.com",
    "password": "Test1234!"
  }
}

script:post-response {
  const body = res.getBody();
  if (body.success) {
    bru.setVar("userId", body.data.user._id);
  }
}
